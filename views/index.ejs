<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <meta content="width=device-width, initial-scale=1" name="viewport" />
    <link rel='stylesheet' href='/stylesheets/index.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <form id="container" action="/randomRestaurant/" method="post">
      <span>Add an URL from Tazz restaurants page</span>
      <input id="restaurantURL" placeholder="Restaurants link" name="url"></input>
      <button id="chooseRestaurant" type="submit">Choose</button>
      <% if (error) { %>
        <span id="error"><%= error %></h2>
      <% } %>
    </form>
  </body>
</html>
